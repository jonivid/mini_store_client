import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  loginBtn: {
    backgroundColor: "#e7507d",
    "&:hover": {
      backgroundColor: "#c30d43",
      // color: "#000000",
    },
  },
}));
